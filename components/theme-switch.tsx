import Switch from "@material-ui/core/Switch";

export default () => {
  return <div></div>;
  return (
    <Switch
      defaultChecked
      color="default"
      inputProps={{ "aria-label": "checkbox with default color" }}
    />
  );
};
